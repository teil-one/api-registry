import { JsonApiRegistry as JsonRegistry } from './JsonApiRegistry';
import { JsonResponseError } from './JsonResponseError';

const JsonApiRegistry = new JsonRegistry();

export { JsonApiRegistry, JsonResponseError };
